%h1 Edit School
%div
  = render 'form'

%span.label.label-important
  %i.icon-trash
  = link_to "Delete School", school_path(@school), class: "button_link", method: "delete", confirm: "Are you sure you want to delete this school?" 